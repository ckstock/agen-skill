# ABAP Conventions（资源层 Resource）

此文件是按需读取的详细资源，不是技能触发入口。

## 选择屏幕

选择屏幕字段描述使用明确形式：

```abap
%_p_m8_%_app_%-TEXT    = 'XXX'.
%_p_kurst_%_app_%-TEXT = 'XXX'.
```

## 交付检查

- 命名和声明位置符合项目约定。
- 选择条件覆盖空值和组合条件。
- 消息类、消息文本和异常路径可定位。
- 程序、类、Include、函数组、消息类、DDIC、权限和配置等传输对象完整。
